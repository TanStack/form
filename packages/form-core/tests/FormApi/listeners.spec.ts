import { describe, it } from 'vitest'

describe('form - listeners', () => {
  it('field change listeners', () => {})

  it('field blur listeners', () => {})

  it('form mount listeners', () => {})

  it('form reset listeners', () => {})
})
