# Vue multi-step wizard example

This example uses Vue 3.6 form composition and scoped `FormGroup` submissions
to validate each wizard step before submitting the complete form.

To run this example:

- `pnpm install`
- `pnpm dev`
