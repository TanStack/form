# Vue large form example

This example shows how Vue 3.6 form composition can split one typed form across
app-level field components, regular components, and reusable field bundles.

To run this example:

- `pnpm install`
- `pnpm dev`
